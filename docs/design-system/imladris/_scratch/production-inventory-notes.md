# Production inventory notes — RetroBoards @ 4efe4e33 (main), inspected 2026-07-14

## Provenance
- Repo: henryperkins/community-forums, branch main, commit 4efe4e33db6475ce9c59190ba82c72cbd7d4b868
- Authority order: DECISIONS.md (v1.6, 2026-07-09) > product/surface specs (DESIGN/ADMIN/USER/COMPOSER v0.8/COMMUNITY) > accepted ADRs (0001–0020) > app contracts/tests > Imladris DS refs.
- CSP (src/Security/SecurityHeaders.php): `default-src 'self'; base-uri 'self'; form-action 'self'; script-src 'self'; style-src 'self'` (+ connect, img 'self' data:). No CDN of any kind. App ships NO webfonts (system-serif fallback); DS previews must self-host fonts to match constraint class.
- Frontend assets (all static, committed): public/assets/{app.css,app.js,composer.js,wysiwyg-composer.js+css (built from src/client/wysiwyg by npm run build:wysiwyg),passkeys.js,tour.js}. Progressive enhancement; `has-js` class gates.

## Feature flags (src/Core/FeatureFlags.php DEFAULTS) — classification
- UNFLAGGED CORE: shell/topbar/rail/inbox/board/thread/post read+write, auth (login/register/forgot/reset/verify), profiles, account settings, setup wizard, errors, privacy page, unsubscribe, health, SEO-neutral pages, mod basics via moderation_queue flag (below), monogram avatars, sessions.
- DEFAULT-ON (GA dates in file): engagement, notifications, email, mentions, search, dms, moderation_queue, community, oauth, presence, announcements, rich_composer, wysiwyg_composer (GA 2026-07-02), drafts, server_drafts (GA 07-02, ADR 0010), uploads, anti_abuse, appeals (GA 07-02, ADR 0007), branding, seo, product_tour, topic_workflow (07-01), tags (07-01), expanded_feeds (07-01), reputation_ledger (07-01), badge_rules (07-02), community_memory (Thread Intelligence GA 07-12), content_references (07-02), polls (06-30), custom_emoji (07-03), slash_giphy (07-02, inert w/o giphy_public_key), split_merge (07-03), profile_media (07-03), board_folders (07-01), bookmark_folders (07-01), saved_feeds (07-01), custom_profile_fields (07-03), account_lifecycle (07-02, ADR 0006), automated_context (07-12), package_registry, package_themes, capabilities, passkeys, provider_registry, invitations (all P5 Gate A, ADR 0018, 07-09), service_secrets, api_tokens, webhooks, first_party_hooks.
- IMPLEMENTED-DARK (default false, real UI exists behind flag): custom_css (ADR 0009), group_dms, link_previews, expanded_files.
- RESERVED-DARK (Gate B; NO invented UI allowed): server_extensions, governance, service_principals, verified_links. (admin nav shows disabled "Extensions" entry w/ note.)
- Flag-independent emergency brake: PACKAGE_EXECUTION_DISABLED. Corrupt `features` JSON → defaults + error_log, admin banner overridesCorrupt.

## Composer shared-shell contract (COMPOSER.md v0.8 + ADR 0013/0020 + composer_shell.php @ commit)
- One server-rendered shell, 4 mounts: reply / new_thread / dm / edit — 8 `.composer-input` forms across the app. Identical feature surface (§15 matrix).
- Form: `form.composer.composer-shell[data-composer-context|target-id|instance]` (+ data-no-draft, data-no-wysiwyg, data-thread-composer). Every form: CSRF `_token` field + hidden fresh `idempotency_key` (server-rendered; ledger `(user, hashed key, context)` replays result; JS in-flight guard is feedback only).
- Anatomy (top→bottom): `.composer-box` (contained: border `--border-strong`, radius-lg, `--surface-raised`, `--shadow-inset`; focus-within → `--gold-400` border + 3px `--focus-ring` halo)
  1. `.composer-format-slot` → `.composer-toolbar` engraved ICON row (28×28 buttons, 17px SVG stroke 1.8 round; hover sunken; active `aria-pressed` gold border + gold-700; `data-tip` tooltips; `.composer-toolbar-sep` hairline groups; GROUP_BREAKS after code + spoiler). Narrow: keeps essentials, `＋` `.composer-more-toggle` "More formatting" overflow menu (`.composer-format-overflow` → strike/code/quote/h2/orderedList/codeblock/spoiler as `.composer-overflow-action` text rows).
  2. `.field-error` (inside box, above input) when validation error.
  3. `.composer-input` textarea (borderless in box, min-height 82px, max-height min(42dvh,360px), pad 10px 12px). WYSIWYG (Milkdown) hides textarea when rich_composer+wysiwyg_composer on; textarea = canonical Markdown, source mode, no-JS fallback. Markdown canonical; never store editor JSON.
  4. `.composer-upload-tray` (grid gap 8): `.composer-upload-card` 48px thumb + name + status + <progress> + alt `.input` + actions (Remove etc); `.is-failed` status danger.
  5. `.composer-actions-bar` (border-top hairline, pad 6px 8px): START = `Aa` `.composer-format-toggle` (toggles toolbar row; persisted rb-composer:format-row; aria-expanded), Attach + Emoji toggles (30px min), `.composer-identity` (24px monogram + "as **Name**", honors show_avatars) , `.composer-anonymous-chip` (checkbox pill; :has(:checked) → gold-soft/gold border; aria-describedby disclosure). END = `.composer-preview-toggle` "Preview" (server render POST /composer/preview → `.composer-preview.formatted-content` pane in [data-composer-after-box]; debounce 350ms; persisted), `.composer-send` 36px CIRCULAR btn, quill glyph ✒ (aria-label = submit label; `.is-submitting` spins quill 700ms; disabled opacity .48).
- `.composer-meta-row` under box (grid auto/1fr/auto, .78rem): `.composer-meta-draft` ("Draft saved" + `.composer-discard` underlined Discard link), `.composer-anonymous-disclosure` ("Your name is hidden from other members; moderators can still see it."), `.composer-count` mono "n / limit" appears ≥90% of limit, `.over` danger.
- Suggestion popovers (`@` mentions, `:` emoji server-backed autocomplete, `#` references boards/tags/topics/posts): `.composer-suggestion-popover` above box; emoji picker = accessible DIALOG `.composer-emoji-dialog` w/ search + grouped grid (unicode + custom emoji). Slash inserts + GIPHY picker (`.composer-slash-*`) when slash_giphy. `.composer-draft-sync` card (server_drafts; `.is-conflict` warning state = local/remote choose-which).
- Keyboard: context-aware desktop Enter-to-send (off inside list/quote/code), Cmd/Ctrl+Enter always sends, Shift+Enter newline, touch soft-Enter = newline; Cmd/Ctrl+B/I/K etc; Esc blurs, draft kept, explicit Discard deletes. NO global r/c/palette keys, NO ↑ edit-last, NO optimistic send (ADR 0020 deferrals — send = full navigation to canonical thread + posted-reply anchor), NO visual replying-to chip (Quote = Markdown insertion).
- Identity line: "as **Name**" (composer_shell.php) — the OLD "Posting as" strip, sentence-case TEXT-button toolbar, standalone textarea + separate action-row card anatomy are SUPERSEDED (v0.7).
- Drafts: localStorage per context key draft:thread:{id}:{uid} etc + server_drafts sync + Drafts view. States: empty→typing→sending→success/error taxonomy (network/not-authed joinbar/banned/no-permission/rate-limited/content-blocked/held-for-approval/too-large/locked-mid-compose).
- Maxlengths: post ~20000, DM ~4000. Counter near-limit. Anonymous where board allows (masked logged-in identity; mods can reveal).
- New Thread wrapper: Title + board picker; DM wrapper: recipient fields (dm_compose_fields.php); Edit: "Save changes" + edit window.

## Admin nav (templates/admin/_nav.php) — production section list
Always: Dashboard /admin · Feature flags /admin/features · Thread Intelligence /admin/thread-intelligence (flags_any community_memory|automated_context) · Boards & categories /admin/structure · Users /admin/users · Branding · Tags · Badge rules · Email · Announcements.
Flag-gated (rendered DISABLED w/ note "Disabled until the feature flag is enabled" when off): API tokens · Webhooks · Packages /admin/packages · Registry trust /admin/registries · Themes /admin/themes · Roles /admin/roles · Sign-in providers /admin/providers · Invitations /admin/invitations · Extensions /admin/extensions (server_extensions=RESERVED → shows disabled).
Admin templates: dashboard, features, thread_intelligence, structure(+structure_confirm, board_edit), users(+user_record), branding, tags, badge_rules(+badge_rule_preview), email, announcements, api_tokens, webhooks(+webhook_detail), packages(+package_detail, package_plan, package_consent, package_security, package_publisher, _package_integration, _package_review_form), registries, themes(+theme_safe_mode), roles(+role_edit, role_simulator), providers(+provider_disable), invitations, extensions.

## Full template inventory (templates/)
Root: home, inbox, board, thread, compose, feed, search, notifications, leaderboard, privacy, unsubscribe, layout, errors/error, setup/wizard.
account/: settings(profile), preferences(reading), appearance, composing, drafts, notifications, security, sessions, privacy, blocks, boards, connections, lifecycle.
auth/: login, register, forgot, reset, mfa, verify. (passkeys.js enhances login/security.)
dm/: index, new, show. mod/: reports, approvals, appeals. appeals/index (member). profile/: show, connections, gated. tags/: index, show.
partials/: composer_shell, composer, new_thread_form, dm_compose_fields, dm_list, dm_rail, post, post_toolbar, thread_row, thread_tools, thread_status_history, thread_memory_tools, thread_restructure, living_brief, topbar, sidebar, settings_nav, monogram, badges, pagination, flash, announcement_banner, icon.

## DS drift found (to fix)
1. Composer.jsx/.d.ts/.prompt.md + components.css `.composer` block = OLD anatomy ("Posting as" strip, text-button toolbar B/I/S…, standalone .textarea, .composer-actions row w/ green send). Consumers: ui_kits/retroboards/Conversation.jsx, templates/council-topic/CouncilTopic.dc.html (x-import posting-as), components/forum/forum.card.html, components/forum/ThreadRow.prompt.md ex., templates/thread-view/ThreadView.dc.html (inline strip), design_handoff_thread_view/{ThreadView.dc.html,README.md}, templates/engineering-handoff/EngineeringHandoff.dc.html FIG2 caption, components/doc/doc.card.html Figure caption, imladris-spec.md Composer line + toolbar desc.
2. --text-body collision: app.css :root `--text-body: var(--ink-700)` (twilight `#D7DCD2`) = semantic COLOR; DS tokens/typography.css has `--text-body: 1.0625rem` SIZE. DS files already use it as color (ui_kits/auth/kit.css L59, templates/engineering-handoff L19, feature-ui/index.html legend, design_handoff_dm_reimagine SOURCE.md) → silently invalid today. FIX: colors.css gains semantic --text-body (+ twilight override); typography.css renames size token → --text-size-body; update size usages.
3. Fonts: tokens/fonts.css = Google Fonts @import (CSP-violating class) → bundle WOFF2 under assets/fonts/ + OFL licenses; rewrite as @font-face.
4. App snapshots in usable source: public/assets/{app.css,app.js,composer.js,tour.js} — compiled INTO _ds_bundle.js today. → archive under _archive/, delete public/. _scratch/wf_confirm.html links it (delete), _scratch/community-forums-DESIGN.md = app doc snapshot (archive).
5. Legacy pull artifacts: root manifest.json (DesignSync pull, wrong id 89e0d236), _ds/imladris-design-system-89e0d236…/ (predecessor bundle), imladris-council-forum-design/ (old handoff w/ superseded composer §5.2 + "Posting as" figs; helmet links _ds/89e0…). → move all to _archive/design-pull-2026-06/; root manifest.json becomes the inspection manifest (commit + unresolved_gaps).
6. Emoji guidance stale: README "Emoji. Not used." + SKILL.md "No emoji." → correct: decorative/status emoji in UI chrome remain prohibited; authored-content emoji + composer emoji tooling (`:` autocomplete, picker dialog, custom emoji, GIPHY via slash) are product features the DS supports. Check guidelines/vocabulary.card.html + voice.card.html.
7. feature-ui/index.html statuses stale ("dark…for want of a place to touch them", 14 flags w/ Flip now/Blocked tags) → now: polls/tags/expanded_feeds/board_folders/bookmark_folders/saved_feeds/topic_workflow/split_merge/community_memory/content_references/custom_profile_fields/reputation_ledger/server_drafts ALL GA default-on; link_previews + expanded_files still dark. Reframe as live-feature designs w/ current flag truth.
8. Admin kit missing production sections: Feature flags, Thread Intelligence, Packages, Registry trust, Themes, Roles, Sign-in providers, Invitations (+ disabled-entry subnav state). Auth kit missing passkeys (register/sign-in/step-up) + invited registration. No setup wizard / error / privacy / unsubscribe / profile-gated representations.
9. README composer/emoji/fonts sections + kit READMEs need reconcile; SKILL.md same.

## Composer build spec (exact, from composer.js @ commit) — for the rewrite
- ACTION_ORDER: bold, italic, strike, code, quote, h2, list, orderedList, codeblock, spoiler, link. Separators (`.composer-toolbar-sep`) AFTER `code` and AFTER `spoiler`. ESSENTIAL (narrow-visible, `.is-essential`): bold, italic, list, link.
- LABELS/shortcuts: Bold Ctrl+B · Italic Ctrl+I · Strike · Inline code Ctrl+E · Quote · Heading · Bullet list · Numbered list · Code block · Spoiler · Link Ctrl+K. aria-label = `Label (Ctrl+X)`; data-tip = `Label · Ctrl+X`; aria-keyshortcuts = `Control+X Meta+X`; aria-pressed on all.
- ICON_PATHS (24×24, stroke inherits .composer-toolbar-action svg: 17px, stroke-width 1.8, round caps):
  bold ['M8 5h5a3 3 0 0 1 0 6H8z','M8 11h6a4 4 0 0 1 0 8H8z']; italic ['M10 5h7','M7 19h7','M14 5 10 19']; strike ['M6 7h10','M5 12h14','M8 17h8']; code ['m9 8-4 4 4 4','m15 8 4 4-4 4']; quote ['M6 7h5v5H7v5','M14 7h5v5h-4v5']; h2 ['M5 6v12','M13 6v12','M5 12h8','M16 10c0-2 4-2 4 0 0 2-4 3-4 6h5']; list ['M9 7h10','M9 12h10','M9 17h10','M5 7h.01','M5 12h.01','M5 17h.01']; orderedList ['M5 6h1v3','M5 13c2-1 2 2 0 3h2','M10 7h9','M10 12h9','M10 17h9']; codeblock ['M5 6h14v12H5z','m9 10-2 2 2 2','m6-4 2 2-2 2']; spoiler ['M3 12s3-5 9-5 9 5 9 5-3 5-9 5-9-5-9-5','M12 10a2 2 0 1 0 0 4 2 2 0 0 0 0-4']; link ['M10 14 8.5 15.5a3 3 0 0 1-4-4L7 9a3 3 0 0 1 4 0','m14 10 1.5-1.5a3 3 0 0 1 4 4L17 15a3 3 0 0 1-4 0','m9 15 6-6'].
- Overflow `＋` `.composer-more-toggle` aria-label "More formatting" → `.composer-format-overflow` (role=group) rows `.composer-overflow-action` (text labels): strike, code, quote, h2, orderedList, codeblock, spoiler.
- Aa `.composer-format-toggle` textContent 'Aa', aria-label "Formatting", aria-expanded, toggles toolbar row (persist rb-composer:format-row; default OPEN).
- Attach `.composer-attach-toggle` textContent '＋', aria-label/title "Attach images". Emoji `.composer-emoji-toggle` textContent '😊', aria-label "Emoji", aria-haspopup dialog. Preview `.composer-preview-toggle` text 'Preview'. Send `.btn.composer-send` circular 36px, span '✒', aria-label = submit label; form.is-submitting → aria-busy + spin (700ms); sr-only status "Sending…".
- Meta: `.composer-meta-draft` "Draft saved" + `.linkbtn.composer-discard` "Discard" (aria-label "Discard draft") · `.composer-anonymous-disclosure` · `.composer-count` "n / limit" (hidden <90%, .over danger).
- Identity: `.composer-identity` = 24px monogram (honor show_avatars) + `.composer-identity-copy` "as <strong>Name</strong>" (dir=auto). Anonymous chip: checkbox+label "Anonymous", :has(:checked)→gold; disclosure "Your name is hidden from other members; moderators can still see it."
- Upload card: 48px `.composer-upload-thumb`, `.composer-upload-name`, `.composer-upload-status` ("Uploading name..." / "Uploaded image WxH." / failed=danger), <progress>, alt `.input` placeholder alt text, actions Up/Down/Remove `.btn.btn-secondary.btn-small`.
- Draft-sync conflict `.composer-draft-sync.is-conflict`: "Draft conflict detected." + muted "A newer server draft exists for this composer." + Keep local / Keep server (btn-secondary btn-small) + "Save local as next revision" (btn btn-small).
- Emoji dialog `.composer-emoji-dialog`: header title "Emoji" + `.composer-emoji-close` '×' (aria-label "Close emoji"), `.composer-emoji-search` type=search + sr-only label "Search emoji", `.composer-emoji-status`, `.composer-emoji-groups` > group > `.composer-emoji-group-title` (uppercase .74rem) + `.composer-emoji-grid-cells` > `.composer-emoji-cell`.
- Media queries: ≤640px → toolbar overflow hidden, non-essential actions + seps hidden, more-wrap inline-flex; popover/dialog fixed bottom sheets; meta-row 2-col w/ disclosure full row. (hover:none),(pointer:coarse) → all toggles/cells 44px min. prefers-reduced-motion → no transitions/spin.
- CSS source block: _archive/app-snapshots/2026-07-14-4efe4e33/app.css lines 985–1415 (composer shell) — port verbatim into components.css replacing old block at lines ~451–467 (old .composer card padding, .composer-id, text-button .composer-toolbar, .composer-actions, .composer-count margin-left:auto). KEEP a `.composer { margin-top: 16px }`? app.css 615 has it. Also add `.field-error` (check app.css for its def — grep) since composer-box > .field-error is referenced; components.css lacks .field-error today.
- components.css already has: .btn-small, .linkbtn, .sr-only, .muted, .badge, .input/.textarea (keep .textarea for generic use — production still uses .composer-input textarea; DM non-shell forms exist e.g. dm-form:not(.composer-shell)).

## Remaining execution plan (in order)
1. components.css: replace old composer block (~L451-467) w/ ported shell CSS + .field-error (copy def from app.css — grep `\.field-error` there) + keep legacy `.composer{margin-top:16px}`.
2. Rewrite components/forum/Composer.jsx (presentational shell recreation: props  + local monoClass/initials helpers like Post.jsx) + Composer.d.ts. NO postingAs/toolbar props (superseded).
3. Update consumers: forum.card.html (member/guest/states demos), ui_kits/retroboards/Conversation.jsx (postingAs→identity), templates/council-topic/CouncilTopic.dc.html (x-import props: identity="Erestor" submit-label="Reply"...), templates/thread-view/ThreadView.dc.html (inline dock: replace "Posting as" strip + text toolbar with shell anatomy: box w/ icon toolbar row, Aa/＋/😊 actions bar, "as Erestor", circular ✒ send), components/forum/ThreadRow.prompt.md example line, components/doc/doc.card.html FIG caption ("sticky composer reads..."→"the shared composer shell with the engraved icon toolbar; identity reads 'as …'"), templates/engineering-handoff/EngineeringHandoff.dc.html FIG2 caption same, imladris-spec.md Composer bullet → shell contract.
4. README.md: Emoji section (decorative/status prohibited; authored emoji + composer emoji tools ARE product features: `:` autocomplete, 😊 picker dialog, custom emoji, GIPHY slash when configured), composer description under kits, fonts caveat → self-hosted note, Sources note re archives, Index → add _archive note, drop handoff folder entries (archived), add feature-ui note. SKILL.md: same corrections ("No emoji" line → nuanced; fonts "Real Google Fonts"→self-hosted OFL).
5. feature-ui/index.html: statuses → GA truth (all 14 except link_previews GA default-on; link_previews + expanded_files implemented-dark; server_drafts GA). Update Lede copy (no longer "dark"), TAG labels → "GA · default-on" / "Dark · default-off". Check sub-pages headers for stale "dark" claims (grep "dark|flip" in feature-ui/*/index.html).
6. Admin kit: add 8 sections (features console w/ flag table+override state+corrupt-overrides banner, thread intelligence, packages catalogue+detail, registry trust, themes+safe mode, roles+simulator, providers, invitations) to ui_kits/admin (read AdminApp.jsx/AdminSections.jsx/data.js first). Add disabled-subnav-entry state (Extensions = reserved-dark shows disabled w/ note). Auth kit: passkeys sign-in/step-up + invited registration states (read AuthApp.jsx). System surfaces: setup wizard, error page (incl. database-down), privacy, unsubscribe, profile-gated — add as ui_kits/system/ or reading kit extras.
7. Docs: PRODUCTION_PARITY.md (matrix from notes: template inventory × flag class × spec × DS coverage), RUNTIME_CONTRACT.md (CSP, PE/no-JS, composer contract, theming data-theme, tokens, fonts self-host, React=preview-only), production-contract.json (machine-readable: commit, flags+classification, surfaces[] w/ route/template/flag/ds_coverage), CHANGELOG.md, manifest.json REWRITE (inspected commit 4efe4e33db6475ce9c59190ba82c72cbd7d4b868, unresolved_gaps: []), README index updates.
8. check_design_system; fix issues; verify a few cards via ready_for_verification (index.html).
9. Zip: present_fs_item_for_download whole project labeled "imladris-design-system-current".

- [ ] Every flag + unflagged surface in parity matrix
- [ ] Every production UI state designed or classified behavior-only
- [ ] No superseded Composer anatomy in source or previews
- [ ] Nothing removed/renamed/invented vs production
- [ ] Responsive/light/dark/system/keyboard/focus/a11y reviews pass
- [ ] Deliverables: PRODUCTION_PARITY.md, RUNTIME_CONTRACT.md, production-contract.json, CHANGELOG.md, manifest(commit, unresolved_gaps:[]), fonts+licenses, zip

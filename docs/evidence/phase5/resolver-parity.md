# Phase 5 - Resolver Parity Corpus (Increment 1, P5-08)

> Generated by `bin/console verify:resolver-parity`. Legacy oracle vs CapabilityResolver on the same fixture and commit.

Commit: `684210f`
Fixture: `phase5_fixture_v2`
Catalogue: 54 keys
Tuples compared: **1551**
Agreed: **1551**
Mismatches: **0**

## Coverage

Every catalogued capability was compared for every fixture actor against the targets its scope calls for. For example, `core.thread.lock` is checked per board per actor.

## Known divergences (recorded, not modeled)

Two production behaviors sit outside both the resolver and this oracle by design; they agree
with each other here and differ from live routes as documented in
`docs/phase5/capability-taxonomy.md` §7 items 5-6:

1. Pending-content *views* (`ApprovalController.php:29`, `MediaController.php:204`) skip the
   state gate in production; the model applies state-beats-role uniformly. Owner decision at
   the Increment 6 route cutover (state-exempt the views vs accept the tightening).
2. Content-state closes (archived/locked/deleted sub-rules in ModerationService,
   ReactionService, SolvedAnswerService, ThreadWorkflowService) are service-owned and keep
   applying unchanged at and after cutover; the resolver models capability-holding only,
   narrowing by content state solely where the legacy route gate was BoardPolicy::canPost.

## Mismatches

None. **Exit-gate criterion met: zero parity mismatch for built-in roles on the critical fixtures.**

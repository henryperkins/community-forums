# A5 mobile account chooser and session labels

Implementation and focused verification are complete. Parent owns combined N6 gates, documentation, and canonical evidence promotion.

## Implementation

- `settings_nav.php` retains the existing single 3-group/feature/active model, with both desktop and mobile calling `settings_nav_links.php`. The mobile native details starts closed with `Settings: {active label}`; the desktop nav stays sticky. Responsive visibility switches at the existing 719/720px account breakpoint. No shared IDs or JavaScript dependency. Fourteen real links remain: thirteen account destinations and Appeals as the intentional shell exit. Tour replay retains its hook and subordinate button.
- Account screens stamp `data-account-settings="1"` via the view section set in the shared settings partial. The existing sidebar receives keyboard focusability on those pages. At <=860px only without `.has-js`, the same real board rail becomes static and scrollable with max-height176px, vertical padding8px, bottom hairline, and app-shell align-content:start. Desktop and enhanced drawer geometry remain unchanged. The bounded rail preserves board, folder, feed and presence destinations; navigating its longer contents requires separate native rail scrolling. This is an explicit ADR0032 adaptation, authorized after inspecting actual geometry; it is not claimed as original prototype fidelity.
- `UserAgentLabel::for(?string): string` examines at most1024bytes with fixed ordered patterns. Edge variants and Opera precede generic Chrome/Safari; Firefox iOS, Chrome iOS, Samsung Internet, Windows/macOS/iOS/Android/Linux are covered. Labels contain fixed literals only, with `Unknown device`, `Unknown browser on {OS}` and `{browser} on unknown OS` fallbacks. No version, invented model, third-party dependency, or security-policy inference.
- SettingsController adds the display label to each session view row. Sessions render that primary label, current-device chip, existing IP/timestamp/revoke controls and expiry note. Raw stored UA remains escaped in native secondary Browser details with wrapping; blank agents show explanatory copy. Session IDs, ownership and revocation service behavior were not changed.
- Added matching CSS to application, Imladris source, design mirror and generated CSS. Refreshed only application_surface.sha256 in baseline, preserving literal reconciled_through_commit; generated manifest was rebuilt.

## RED / GREEN evidence

- Initial focused PHP20tests:19 missing-class errors for the new unit API, one expected integration failure because Chrome on Windows was not rendered. `/tmp/a5-red-php.log` records this precursor, not a product pass.
- Initial authenticated noJS390x844 browser RED:Security first field bottom1631.4375px. Board rail geometry y62,height723.171875px. Opened the failing screenshot before implementing the account-scoped cap. An earlier setup-only attempt hit local WEBAUTHN_RP_ID mismatch; rerun with localhost produced the actual product RED.
- Final focused PHP:91tests,773assertions,PASS,1.158s. Group:UserAgentLabelTest,AppAccountConsoleTest,AppSessionManagementTest,AppImladrisRuntimeTest,ImladrisRuntimeAssetTest. Tests exercise labels/overlap/unknown/100KB malformed input, raw escaping, visible-current revocation controls, another-user ownership, revoke-one/others/current, password-change revocation, all13guest redirects, same flags/order/current state across both rendered navigations, account contracts, and generated runtime assets.
- Final broad account-console browser run:17passed,12intentional viewport-specific skips; the added six-context stress test exceeded default30s after24captures were added. Its prior shorter-capture run passed all behavioral assertions. The final isolated stress rerun passed1/1 in38.7s (40.1s process), with a90s per-test budget for six browser contexts/two themes/24captures. No429/authentication-rate failures appeared in either run. No functional assertion was weakened. An earlier desktop strict-selector failure from two responsive navs was corrected to evaluate the accessible visible nav. A PHP test-only Location header casing error was corrected to use getHeader('location').
- Initial noJS390x844 first-control bottoms in both light and dark:Security613.828125px,Profile714.390625px,Notifications693.015625px. All measured before scrolling or opening the chooser. Final captures are viewport-only.
- Browser coverage:all14 ordinary hrefs without JS; exact nav groups/current state; flags off individually; desktop sticky geometry; keyboard Enter/Space opening/closing, Tab to Profile and then from closed summary to first Security field; long unbroken labels; widths320/390/640 with JS on/off and light/dark; bounded board rail focus and last-link visibility; 422 retention; profile/security/panes Axe in light/dark/system; sessions escaped malicious/long/blank raw headers and actual noJS revoke-one/others with current token retained.
- 200% is CSS reflow emulation:640x500 CSS viewport with deviceScaleFactor2, equivalent layout space to1280x1000 at200%. Native browser zoom was not manipulated.
- `composer build:imladris`, `composer check:imladris`, source/mirror comparison and `git diff --check` pass. No full PHPUnit suite was run, per parent's ownership.

## Captures reviewed

Opened the original failing full-page Security capture; final initial Security/Profile/Notifications viewport captures; desktop/mobile raw-session details; and320px dark long-label chooser. The first session captures exposed accumulated test login rows, so the session fixture now signs out prior devices before seeding its exact three diagnostic rows, recaptures from the top and cleans those rows in finally. Raw text is visibly escaped and wraps without overlap; unknown labels and current-device chip are retained. Long-label chooser wraps without horizontal overflow.

Artifacts remain isolated under `.superpowers/sdd/2026-09-20-unified-notifications/browser-a5/`; selected final evidence and logs are copied to `docs/evidence/unified-notifications-and-settings/a5/` for parent promotion. Parent's untracked n5 evidence was untouched.

## Commands and handoff

PHP runs use `flock /tmp/retroboards-unified-phpunit.lock env MAIL_DRIVER=sendmail MAIL_FROM='' vendor/bin/phpunit --filter 'UserAgentLabelTest|AppAccountConsoleTest|AppSessionManagementTest|AppImladrisRuntimeTest|ImladrisRuntimeAssetTest'`.

Browser preparation uses isolated DB_DATABASE=retroboards_unified_e2e, MAIL_DRIVER=array, MAIL_FROM=browser@retro.test, E2E_PORT=8034 and tests/browser/prepare.sh. Browser commands also pin WEBAUTHN_RP_ID=localhost and RB_EVIDENCE_DIR=.superpowers/sdd/2026-09-20-unified-notifications/browser-a5, then run account-console.spec.ts with tests/browser/playwright.config.ts. No real email or OAuth ceremony was exercised.

Parent collateral:notifications-unified.spec.ts:222 needs to open a visible closed `[data-settings-mobile-nav]` before clicking its already-visible-scoped replay button. No replay selectors occur in unified-chrome.spec.ts. Parent assigned this to the N5 follow-up implementer alongside the separately queued320px header clipping correction. A5 does not edit those files. Parent must rerun its account-settings-repairs, GateA, board-index and final broad browser/PHP gates against combined code.

Final session-fixture recapture and noJS actions passed2/2 (desktop/mobile),14.3s. The stress and session reruns supersede the timeout/capture issues from the17-passed broad run; no single clean rerun of the entire account-console spec is claimed. Parent N6 owns that combined rerun. Selected final evidence/logs are now under docs/evidence/unified-notifications-and-settings/a5/.

Committed e8e41c9c on parent429b8f91:exactly17owned runtime/test/build paths. Reset retroboards_unified_e2e to the standard seed with prepare.sh after final captures; cleanup succeeded. No live8034listener remains. Source and browser ownership released to parent; no full-suite or deployment claim. HeadlessChrome intentionally falls through to Unknown browser on Linux in Chromium automation captures; normal Chrome headers are recognized and the raw text remains available.

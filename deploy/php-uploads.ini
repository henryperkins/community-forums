; Application policy is 5 MiB; transport allows useful validation responses.
upload_max_filesize=8M
post_max_size=10M
; PHP can warn before dispatch when it discards an oversized POST. Keep the
; controller's JSON response intact and send diagnostics to the server log.
display_errors=Off
display_startup_errors=Off
log_errors=On
